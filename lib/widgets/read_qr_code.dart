import 'package:flutter/material.dart';

class ReadQRcode extends StatelessWidget {
  const ReadQRcode({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('This is Read QR code'),
    );
  }
}
